import rolando.*
import moradas.*
import armas.*

test "libro" {
	
	rolando.poderBase(5)
	rolando.recolectar(libro)
	
	castillo.loot(#{espada, armadura, collar})
	
	libro.hechizos([bendicion, invisibilidad,invocacion])

	assert.equals(4, libro.poderBrinda(rolando))
	rolando.batalla()
	assert.equals(6, libro.poderBrinda(rolando))
	rolando.batalla()
	assert.equals(7, libro.poderBrinda(rolando))
	rolando.batalla()
	assert.equals(0, libro.poderBrinda(rolando))
	
}